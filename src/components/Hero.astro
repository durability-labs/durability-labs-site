<section
  class="min-h-screen flex flex-col justify-center items-center px-6 pt-16 relative"
>
  <div class="max-w-4xl mx-auto text-center">
    <p
      class="font-body text-xs tracking-[0.2em] uppercase text-text-muted mb-6"
    >
      Ethereum Infrastructure Consultancy
    </p>

    <h1
      class="font-display text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-bold text-white leading-[1.1] tracking-tight mb-8"
    >
      We build the infrastructure<br class="hidden sm:block" /> the ecosystem
      relies on
    </h1>

    <p
      class="font-body text-lg md:text-xl text-text-secondary max-w-2xl mx-auto mb-12 leading-relaxed"
    >
      Core contributors to Ethereum's data availability layer, libp2p, and IPFS.
      We design and ship decentralized systems that endure.
    </p>

    <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
      <a href="#contact" class="btn-primary">Start a Conversation</a>
      <a href="#services" class="btn-secondary">View Our Services</a>
    </div>
  </div>

  <!-- Scroll indicator -->
  <div
    class="absolute bottom-12 left-1/2 -translate-x-1/2 scroll-indicator"
  >
    <svg
      width="20"
      height="20"
      viewBox="0 0 24 24"
      fill="none"
      stroke="#444444"
      stroke-width="1.5"
    >
      <path d="M7 13l5 5 5-5M7 6l5 5 5-5"></path>
    </svg>
  </div>

  <!-- Subtle radial gradient background -->
  <div
    class="absolute inset-0 -z-10"
    style="background: radial-gradient(ellipse 60% 50% at 50% 30%, rgba(255,255,255,0.02) 0%, transparent 100%);"
  >
  </div>
</section>
